export interface tokens {
  authToken: string;
  refreshToken: string;
  userId: string;
  roles: string;
  sim: string;
  coreId: string;
  coreSim: string;
  coreName: string;
  location: string;
  code_expiry: string;
}
